const Home = () => {
  return (
    <div className="flex flex-col gap-3">
      <div className="flex items-center gap-1.5">
        <p className="text-sm font-semibold font-expose text-muted-foreground">
          Save to
        </p>
      </div>
    </div>
  );
};

export default Home;
